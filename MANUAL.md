# Activeチャプター 名簿システム ご利用マニュアル

このマニュアルは、BNI Activeチャプターの定例会準備システムの使い方を、はじめての方にもわかるようにまとめたものです。

このシステムでできることは、大きく3つです。

1. **ビジターリストの作成** ... Spreadingからダウンロードしたデータを、きれいな名簿PDF にする
2. **ルーム割り振り表の作成** ... ブレイクアウトルームの担当をドラッグ＆ドロップ（またはAI）で決める
3. **案内メールの一括送信** ... ビジターへの案内メールをまとめて送る

すべての操作は、Googleスプレッドシートの上のメニューバーにある **「名簿システム」** から行います。

---

> 💡 **このマニュアルはスプレッドシートからいつでも開けます**
> メニュー `名簿システム` > `❓ 使い方（ヘルプ）` をクリックしてください。
> 全機能の一覧は `名簿システム` > `🏠 メニュー画面をひらく` から開けます。

---

## 準備：メニューが出ていないところから

初めて使うときや、別のパソコン・別のアカウントで開いたときは、まず
**メニューバーに「名簿システム」を表示させる**ところから始めます。

### ステップ 0：スプレッドシートを開く

共有されたスプレッドシートのURLを開きます。
**ふだんお使いのGoogleアカウントでログインした状態**で開いてください。

> 複数のGoogleアカウントをお使いの場合、別のアカウントで開いていると
> メニューが出ないことがあります。画面右上のアイコンで、今どのアカウントで
> 開いているかを確認できます。

### ステップ 1：メニューバーに「名簿システム」が出るまで待つ

スプレッドシートを開いた直後は、上部のメニューバーに
`ファイル` `編集` `表示` … と並んでいるだけです。

数秒待つと、いちばん右あたりに **「名簿システム」** が追加されます。

```
ファイル  編集  表示  挿入  表示形式  データ  ツール  拡張機能  ヘルプ   名簿システム
                                                                      ↑ここに出ます
```

**出てこないときは、ブラウザの再読み込み（F5 または Ctrl+R）をしてください。**
読み込みのたびにメニューが作られるしくみのため、再読み込みで解決することがほとんどです。

### ステップ 2：それでも「名簿システム」が出ないとき

以下を順に確認してください。

| 確認すること | 対処 |
|---|---|
| ログインしているアカウントは合っていますか | 画面右上のアイコンで確認し、正しいアカウントに切り替える |
| このスプレッドシートの閲覧権限しかありませんか | 編集権限がないとメニューは動きません。管理者に編集権限を依頼してください |
| ブラウザは Google Chrome ですか | Chrome を推奨します。他のブラウザでは正しく動かないことがあります |
| シークレットウィンドウで開いていませんか | 通常のウィンドウで開き直してください |
| 拡張機能でブロックされていませんか | 広告ブロッカー等を一時的に無効にして再読み込み |

それでも出ない場合は、`拡張機能` > `Apps Script` を開いてみてください。
スクリプトの画面が開けば、ファイルは正しく入っています。開けない場合は権限の問題です。

### ステップ 3：最初の1回だけ、Googleの許可が必要です

メニューが出たら、何か機能をクリックしてみてください。
**初回だけ**「承認が必要です」という画面が出ます。次の章の手順どおりに進めてください。

---

## はじめに：初回だけ必要な「Google認証」の手順

システムを初めて使うとき、Googleから「このスクリプトを動かしてもいいですか？」という確認が出ます。

これは**セキュリティのための正常な動作**です。システムがあなたの代わりにPDFを作ったりメールを送ったりするために、あなたの許可が必要なのです。

ちょっとビックリする画面が出ますが、以下の手順どおりに進めれば大丈夫です。

---

### 手順（スクリーンショット付き解説）

**ステップ 1**

メニューから何かの機能（例えば「1. CSVから名簿・PDF作成」）をクリックすると、こんなポップアップが出ます。

```
┌─────────────────────────────────┐
│                                 │
│     承認が必要です              │
│                                 │
│  このプロジェクトがあなたの     │
│  データへのアクセスを必要と     │
│  しています。                   │
│                                 │
│        [ 続行 ]                 │
│                                 │
└─────────────────────────────────┘
```

→ **「続行」をクリック** してください。

---

**ステップ 2**

Googleアカウントの選択画面が出ます。

→ **ふだん使っているGoogleアカウントをクリック** してください。

---

**ステップ 3**

ここが一番ドキッとするところです。こんな警告が出ます。

```
┌─────────────────────────────────────┐
│                                     │
│   ⚠ Google はこのアプリを          │
│     確認していません                │
│                                     │
│  このアプリは、Googleによる確認が   │
│  済んでいないため...                │
│                                     │
│                                     │
│                  ▶ 詳細             │
│                                     │
└─────────────────────────────────────┘
```

→ 怖く見えますが大丈夫です。左下の **「詳細」** をクリックしてください。

（この警告は、Googleの公式審査を通していない自作スクリプトすべてに出るもので、危険だから出ているわけではありません）

---

**ステップ 4**

「詳細」を押すと、下に小さな文字のリンクが現れます。

```
  ▼ 詳細

  〇〇〇（安全ではないページ）に移動
```

→ **「〇〇〇（安全ではないページ）に移動」をクリック** してください。

（「安全ではない」と書いてありますが、これも自作スクリプトには必ず出る表現です）

---

**ステップ 5**

最後に、このシステムが使う権限の一覧が表示されます。

```
┌──────────────────────────────────────┐
│                                      │
│  「名簿システム」が Google           │
│  アカウントへのアクセスを            │
│  リクエストしています                │
│                                      │
│  ☑ スプレッドシートの読み書き       │
│  ☑ Google ドライブへのアクセス      │
│  ☑ Gmail でのメール送信             │
│  ☑ 外部サービスへの接続             │
│                                      │
│           [ 許可 ]                   │
│                                      │
└──────────────────────────────────────┘
```

→ **「許可」をクリック** してください。

---

### これで完了です

この認証は **最初の1回だけ** です。次回からはこの画面は出ません。

### 認証のあと、最初にやること

メニュー `名簿システム` > **`🏠 メニュー画面をひらく`** をクリックしてください。
すべての機能が一覧で表示され、右側の **「準備の状況」** に、何が設定済みで何が未設定かが
✓ と ! で表示されます。! が付いている項目は、その場のリンクから設定画面を開けます。

> **追加の許可を求められることがあります**
>
> スライド作成や大きなファイルを扱う機能は、最初に使うときにさらに許可が必要な場合があります。
> 画面に許可を求めるダイアログが出たら「許可」を選んでください。
>
> なお、設定画面の中で「サーバーエラー」と表示された場合は、許可がまだ取れていない可能性があります。
> その場合はメニュー `名簿システム` > `🔧 動作確認` から
> **`Googleの権限を確認・許可する`** または **`Gemini接続テスト`** を1回実行してください。
> （ダイアログの中からは許可を求める画面を出せないため、メニューから直接実行する必要があります）

> **それぞれの権限が何に使われるか**
>
> | 権限 | 使う場面 |
> |------|----------|
> | スプレッドシートの読み書き | 名簿データの保存や読み込み |
> | Google ドライブへのアクセス | PDFファイルの作成と共有 |
> | Gmail でのメール送信 | ビジターへの案内メール送信 |
> | 外部サービスへの接続 | AI自動割り振り（Gemini API）の呼び出し |
> | Google ドキュメントへのアクセス | メンバーリストPDFの文字読み取り（OCR） |

---

## メニューの構成

メニューは、よく使うものほど手前に来るように整理しています。

```
名簿システム
├─ 🏠 メニュー画面をひらく      ← 全機能の一覧と、準備状況の確認
├─────────────────
├─ 1. CSVから名簿・PDF作成        ┐
├─ 2. メールの確認・一括送信      │ 毎週の作業
├─ 3. ルーム・オリエン割り振り表  │
├─ 4. 作成済みPDFの確認           ┘
├─────────────────
├─ 📊 スライド・冊子をつくる ▸    ビジター/定例会スライド、メンバーブック、Zoom案内
├─ 🗂 シートの整理 ▸              タブが増えたときの整理と復元
├─ ⚙️ 設定 ▸                      初回や内容が変わったときだけ触ります
├─ 🔧 動作確認 ▸                  うまく動かないときの点検
├─────────────────
└─ ❓ 使い方（ヘルプ）            このマニュアル
```

▸ が付いている項目にマウスを合わせると、中身が開きます。

**どこにあるか分からなくなったら、`🏠 メニュー画面をひらく` を使ってください。**
全機能がボタンで並び、右側に「準備の状況」（何が設定済みか）も表示されます。

---

## 毎週の作業の全体の流れ

定例会の準備は、毎週この順番で進めます。

```
① Spreadingで「CSVダウンロード」
        ↓
② 名簿システムで「CSVから名簿・PDF作成」
        ↓
③ 名簿システムで「ルーム・オリエン割り振り表」
        ↓
④ 名簿システムで「メールの確認・一括送信」
        ↓
    準備完了！
```

新しい開催日の名簿を作ると、**前の開催日のシートは自動で非表示（アーカイブ）**されるため、
タブが増え続けることはありません。詳しくは「シートの整理（アーカイブ）」をご覧ください。

以下、それぞれの手順をくわしく説明します。

---

## ステップ①　Spreadingからデータをダウンロードする

1. **Spreading** にログインします。
2. 対象の定例会を選択します。
3. **「CSVダウンロード」** をクリックして、パソコンに保存します。

> **ポイント**: ダウンロードしたCSVファイルは、そのまま触らずに使ってください。Excelで開いて保存し直すと文字化けすることがあります。

> **AI連携（MCP）ではこの手順を置き換えられません。**
> SpreadingのAI連携でも参加者は取得できますが、
> **メモ（ビジターリストに表示）・オリエンテーション希望・決裁権・決裁スピード・入会見込み・役職・ウェブサイト**
> が含まれておらず、CSVより項目が少なくなります。
> 毎週の参加者はこれまでどおりCSVを使ってください。
> AI連携が役に立つのは、**メンバー名簿の更新**（「Spreadingから名簿を更新」）のほうです。

### 「つなげたいメンバー」のメモ

Spreadingの仕様上、**「紹介したい（つなげたい）メンバー」の情報はCSVに含まれません。**

そのため、Spreadingの画面で各ビジターの詳細を開き、つなげたいメンバーの名前をメモしておいてください。後の「割り振り表」のステップで使います。

---

## ステップ②　ビジター名簿とPDFを作成する

**メニュー**: `名簿システム` > `1. CSVから名簿・PDF作成`

### 手順

1. メニューから **「1. CSVから名簿・PDF作成」** をクリックします。
2. **定例会を選ぶ**: 上のプルダウンで、対象の定例会（日付と回数）を選びます。
3. **CSVを読み込む**: ダウンロードしたCSVファイルを点線の枠にドラッグ＆ドロップして、 **「全データを解析して編集画面へ」** を押します。
4. **データを確認・修正する**: 編集画面が開きます。

### 編集画面の見方

| 色 | 意味 |
|----|------|
| **赤いセル** | 名前にスペースが入っていなかったため、システムが自動で推測して入れています。正しいか確認してください |
| **黄色いセル** | 招待者の名前がメンバーリストと完全一致しなかったため、推測しています。確認してください |
| 色なし | 自動処理で問題なかったセルです |

どのセルも、直接クリックして文字を書き換えることができます。

### 並べ替えと番号の振り直し

- **「ふりがなでソート」ボタン**: ふりがなの修正後に押すと、正しいふりがな順に並べ替えてV01、V02...の番号も振り直されます。
- **行のドラッグ移動**: 左端の ☰ マークをドラッグすると、行の順番を自由に入れ替えられます。入れ替え後、V01、V02...が自動で振り直されます。

### 確認が終わったら

**「この内容でシートとPDFを作成する」** をクリックします。

→ スプレッドシートにデータが書き込まれ、同時に **誰でも閲覧できるPDF（ビジター様リスト）** が自動作成されます。完了すると、PDFへのリンクが表示されます。

### やり直したいときは

一度作成した名簿を修正したくなった場合、CSVを読み込み直す必要はありません。

画面下部の **「作成済みデータを再編集」** のプルダウンから対象のシート（例: `0408参加者`）を選んで **「このシートを読み込んで再編集」** を押せば、前回のデータがそのまま編集画面に読み込まれます。修正して再度「作成する」を押せば、同じPDFファイルが上書き更新されます（URLは変わりません）。

> **補足**: CSVの列名が日本語でも英語でも、どちらでも自動的に読み込めます。

---

## ステップ③　ルーム・オリエン割り振り表を作成する

**メニュー**: `名簿システム` > `3. ルーム・オリエン割り振り表`

### 手順

1. メニューから **「3. ルーム・オリエン割り振り表」** をクリックします。
2. 対象の定例会を選択して **「データを読み込む」** を押します。

### 画面の説明

#### 上部：ダッシュボード

ビジター・ゲスト・代理の人数、ルーム数、1ルームの平均人数、未配置メンバー数がひと目でわかります。

#### ビジターのカード

ビジターごとに1枚のカードが表示されます。各カードには以下があります。

- **▼ ボタン（全情報）**: ビジターの確度・性格・メモなど、CSVの全情報をその場で確認できます
- **つなげたいメンバー欄**: Spreadingで確認した「つなげたい人」をここに入力します。改行で区切って複数名入れられます。枠の外をクリックすると、自動でその人がルームメンバーに追加されます
- **ファシリテーター枠**: このルームの進行役を配置する場所
- **ルームメンバー枠**: ルームに入るメンバーを配置する場所
- **オリエンテーション枠**: オリエン担当を配置する場所

#### 下部：メンバーの待機リスト（プール）

まだどのルームにも配置されていないメンバーが並んでいます。

#### ドラッグ＆ドロップで配置

- プールからメンバーのチップ（名前の書かれた小さな札）をドラッグして、各枠にドロップします
- 配置済みのチップも、別のルームの枠にドラッグして移動できます
- 使用済みのメンバーはプール上でグレーになります

#### 合同ルーム

ビジター同士を同じ部屋にする場合、ファシリ枠の上のプルダウンから「合同先」を選びます。

### AI自動割り振り（Gemini AI）

手作業が面倒な場合は、AIにお任せすることもできます。

1. **最大人数**（1ルームあたり、デフォルト5名）を指定します。
2. **「AIに提案させる」** ボタンを押します。
3. 数分待つと、AIが全ルームの配置を自動で提案します。

AIは以下の情報を使って最適な配置を考えます。
- メンバーブックの内容（誰がどんなビジネスをしているか）
- ビジターのメモや確度
- つなげたいメンバーの情報

> AI提案後も、気に入らない部分があればドラッグ＆ドロップで自由に調整できます。

### 作成

**「この内容でシートとPDFを作成する」** を押すと、次の3枚のシートとPDFが一度に作られます。

| シート | 内容 |
|---|---|
| `MMDD割り振り表` | 割り振り表の本体。PDFもこのシートから作られます |
| `MMDDオリエン` | ブレイクアウトルーム表（オリエンテーション） |
| `MMDDオープンネット` | ブレイクアウトルーム表（オープンネットワーキング） |

#### ブレイクアウトルームの表について

Zoomのブレイクアウトルームを作るための表です。定例会ごとに2枚できます。
**ルーム名は `V01山田 太郎(カテゴリー)様ルーム` の形**で、割り振り表と同じ割り当てを
ルーム単位で並べ直したものです。

| シート | 列 |
|---|---|
| オリエン | ルーム名 ／ オリエンテーション担当 |
| オープンネット | ルーム名 ／ 招待者 ／ ファシリテーター ／ ルームメンバー |

貼り付け先の列の位置が変わらないよう、**A列は空けたまま、B列をルーム名にしてあります。**

> **合同ルームにしたビジターについて**: 「【合同】〇〇様と同室」としたビジターも、
> BOR表には1行として出ます（ファシリテーター欄に「【合同】…と同室」と表示されます）。
> Zoom側では1つのルームにまとめてください。

割り振りをやり直して作り直すと、この2枚も毎回作り直されます。

---

## ステップ④　案内メールを一括送信する

**メニュー**: `名簿システム` > `2. メールの確認・一括送信`

### 事前に確認

スプレッドシートの下のタブで **「〇〇参加者」シート（例: `0408参加者`）を表示した状態** で実行してください。このシートのデータをもとにメールが作られます。

### 手順

1. メニューから **「2. メールの確認・一括送信」** をクリックします。
2. 全員分のメールプレビューが表示されます。
   - 宛先、件名、本文がすでに入っています
   - 種別（Visitor / Guest / Substitute）がバッジで表示されます
3. **修正したい場合**: 本文のテキストエリアを直接書き換えられます。
4. **送信しない人がいる場合**: その人の **「送信する」チェックを外す** だけです。
5. **「上記の内容で順番に送信する」** をクリックすると、プログレスバーが進みながら1件ずつ送信されます。

> **安心ポイント**: 途中で1件エラーが出ても、残りのメールは止まらず送信されます。エラーは画面に赤字で表示されるので後から確認できます。

---

## 作成済みPDFをもう一度見たいとき

**メニュー**: `名簿システム` > `4. 作成済みPDFの確認`

直近で作成した以下のPDFリンクをワンクリックで開けます。

- ビジター様リスト PDF
- ルーム割り振り表 PDF
- メンバーブック PDF

---

## スライド・メンバーブックの作成

### 事前の準備（最初に1回だけ）

1. **⚙️ 設定 > BNI 素材フォルダ** … 共有フォルダのURLを登録します。その直下に
   `01_テンプレート` `02_メンバー写真` `03_生成物` が自動作成され、以後の素材と
   生成物はすべてここに保存されます。
2. **⚙️ 設定 > メンバー名簿** … 全ツール共通のメンバー情報です。
   「一括取り込み」から、現行のメンバーブックHTML、または表計算からの貼り付けで登録できます。
   この**並び順が掲載順・発表順**になります。
3. **⚙️ 設定 > メンバー写真** … 写真をアップロードすると、**氏名で自動照合**されます。
   ファイル名は「氏名.jpg」または「氏名__識別子.png」にしてください。
   形式は **JPEG・PNG・GIF・WebP** に対応しています（拡張子の大文字小文字は問いません）。
   同じ人の写真が複数あるときは**更新日時が新しいもの**が使われます。
   iPhoneの **HEIC** は表示できないため、JPEGかPNGに変換してください
   （対応していない形式があると、取り込み時にファイル名を挙げてお知らせします）。
4. **⚙️ 設定 > PowerPointテンプレート** … ビジター用の3種類を登録します。
   登録時にレイアウトが自動検証されます。
5. **⚙️ 設定 > 大きなスライド** … 定例会スライドなど数十MBのものは、
   アップロードではなく**Driveの共有リンク**で登録します。

> 大きなファイルを扱う機能は、初回だけメニュー「**🔧 Googleの権限を確認・許可する**」を
> 1回実行してください。権限が未取得だと「サーバーエラー」になります。

### 6. ビジター・代理スライド作成

**メニュー**: `名簿システム` > `📊 スライド・冊子をつくる` > `ビジター・代理スライド作成`

参加者シートから直接読み込むため、**Excelのアップロードは不要**です。

- 🎤 プレゼンスライド … ビジター1人1枚
- 👥 紹介スライド … ビジター3人1枚
- 🟠 代理紹介スライド … 代理3人1枚

No.が「代理」で始まる行が代理、それ以外がビジターとして振り分けられます。
並び順はシートのままです。

### 7. 定例会スライドの自動更新

**メニュー**: `名簿システム` > `📊 スライド・冊子をつくる` > `定例会スライドの自動更新`

開催日を選ぶと、**メンバー名簿の日付から自動で算出**した内容が入ります。

| 項目 | 算出元 |
|---|---|
| 開催回・開催日 | 休会日を除いた次回開催日 |
| 新メンバー | 入会日が開催日と同じ月のメンバー |
| 更新メンバー | 更新日が開催日と同じ月のメンバー（継続年数も入会日から算出） |
| 更新状況一覧 | 更新期限日までの残り日数で 90日／60日／30日以内・期限超過に区分 |
| 実績数値 | 前回入力した値 |

#### 更新の確認がとれた方を一覧から外す

更新状況一覧の下に、対象の方が並びます。それぞれ **「更新した」「更新しない」** を
押すと、上の一覧から外れます。押し間違えたら「戻す」で元に戻ります。

**「更新した」の印は、そのときの更新期限日に対するものです。**
あとで会費レポートを取り込んで期限が伸びると、印は自動的に外れます。
そのため、次の更新時期が来たときはまたちゃんと一覧に出ます。印の消し忘れは起きません。

**「更新しない」**（退会される方）は、期限に関係なく一覧から外れます。
名簿からの削除は行わないので、退会が確定したら「メンバー名簿」から手で消してください。

PowerPointは**Googleスライドに変換せず**、そのまま加工します。テンプレート側で
差し替えたい箇所を `{{開催回}}` のように書いておくと、その部分が置き換わります。
どこに差し込めるかは「⚙️ 設定 > 大きなスライド」の**「差し込み口を調べる」**で確認できます。

### 8. メンバーブックの編集・PDF出力

**メニュー**: `名簿システム` > `📊 スライド・冊子をつくる` > `メンバーブックの編集・PDF出力`

カードをクリックすると編集できます。掲載順は↑↓、または番号を指定して移動できます。

- **🖨 冊子を印刷（PDF）** … 印刷画面が開くので「PDFに保存」を選びます
- **配布用HTMLを保存** … 単一HTMLとして `03_生成物` に保存します

写真はDriveから氏名で自動的に読み込まれます。

### 9. Zoom入室案内の作成

**メニュー**: `名簿システム` > `📊 スライド・冊子をつくる` > `Zoom入室案内の作成`

メンバー名簿の氏名を6列・縦読みで並べたA4横の案内を作ります。
Zoomの表示名を **「番号（半角）名前（カテゴリー）」** にそろえてもらうための案内です。

例に出す番号と人数表記を変えられます。
**番号は名簿のNoをそのまま使います。**並び順から振り直すと、Noが飛んでいるときに
実際の番号とずれてしまい、案内の紙自体が間違うことになるためです。

#### カテゴリーの表示

既定では、全員の欄に **「（カテゴリー）」** とひな形が入ります。
名前の付け方を示すための案内なので、実際のカテゴリーは出しません。

各メンバーの実際のカテゴリーを載せたい場合は、画面のチェックを入れてください。
ただし**カテゴリーが長い方は途中で切れます**（1行に収める作りのため）。
ふだんは既定のままで問題ありません。

---

## シートの整理（アーカイブ）

**メニュー**: `名簿システム` > `🗂 シートの整理` > `アーカイブして整理する`

開催日ごとに「参加者」「参加者_印刷用」「割り振り表」「オリエン」「オープンネット」の
5つのシートが作られるため、何週も続けるとタブが増えていきます。
この画面でまとめて片付けられます。

アーカイブは**開催日単位**です。同じ日付の5枚がまとめて非表示になり、まとめて戻ります。

**アーカイブ＝シートを非表示にするだけです。データは消えません。**
非表示のままでも、再編集・PDFのみ再作成・割り振り表の作成はこれまでどおり使えます。

### できること

| 操作 | 内容 |
|---|---|
| 自動アーカイブ（既定ON） | 新しい開催日の名簿や割り振り表を作ると、それ以外の開催日を自動で非表示にします |
| 最近N件を残して一括アーカイブ | 溜まったタブを一気に整理します（1〜10件から選択） |
| 選択をアーカイブ / 表示に戻す | 開催日ごとに個別に切り替えます |
| 全て表示に戻す | アーカイブ済みをすべて元に戻します |

### 元に戻したいとき

- メニュー `名簿システム` > `🗂 シートの整理` > `アーカイブを全て表示に戻す` … ワンクリックで全部戻します
- または上記の画面で、戻したい開催日にチェックを入れて「選択を表示に戻す」

### 安全のための仕組み

- 「メンバーリスト」「休会日」など、開催日以外のシートはアーカイブされません
- すべてのシートが非表示になる操作はできません（1枚以上は必ず表示されます）
- 自動アーカイブは `5. シートの整理（アーカイブ）` の画面でいつでもOFFにできます

---

## 初期設定（初回・メンバー変更時のみ）

ふだんの毎週の作業では触りませんが、初回セットアップやメンバーの変更があったときに必要な設定です。

### メンバー名簿（すべての機能の正本）

`名簿システム` > `⚙️ 設定` > `メンバー名簿`

メンバーの情報は、この**「メンバー名簿」シート1枚**にまとまっています。
割り振り表もメンバーブックもスライドも、すべてここを見ています。
（以前あった「メンバーリスト」シートは不要になりました。
「旧メンバーリストから移行」ボタンで中身を移し、シートは非表示にできます）

列は次のとおりです。

| 列 | 内容 | どこから埋まるか |
|---|---|---|
| No | メンバー番号 | Spreading / メンバーリスト(OCR) |
| 業種区分 | 企業サポート、不動産関連など | Spreading / メンバーリスト(OCR)の見出し |
| 氏名 / ふりがな | | Spreading / メンバーリスト(OCR) |
| カテゴリー | 生命保険(法人) など | Spreading / メンバーリスト(OCR) |
| 会社名 / 役職 | 役職には「ビジターホスト」等が入ります | Spreading / メンバーリスト(OCR) |
| メモ | 役職と同じく「ビジターホスト」等が入ることがあります | メンバーリスト(OCR) |
| 写真ファイル名 | | メンバー写真の自動照合 |
| 一言コメント / 紹介してほしい人 / 協業したい人 | メンバーブックに載る内容 | 手入力、またはメンバーブックHTMLの取り込み |
| 入会日 | | Spreading / メンバーシップ期間レポート |
| 更新日 | | 手入力 |
| 更新期限日 | | 会費レポート |

#### 一括取り込み（4つの方法）

画面上部の「一括取り込み」から、次の方法でまとめて埋められます。
**Spreadingが使える場合は、次項の「Spreadingから名簿を更新」がいちばん確実です。**
下の4つは、それを補う形で使ってください。

1. **メンバーブックHTML** … 現行のメンバーブック管理ツールで保存したHTMLから、
   カテゴリー・会社名・一言コメント・紹介/協業を取り込みます。
2. **BNI公式レポート(.xls)** … 2つのレポートを選ぶと、氏名で照合して日付を埋めます。
   - メンバーシップ期間レポート → **入会日**（Recent Start date＝最新の入会日）
   - 会費レポート → **更新期限日**（会費がいつまでか）
   - 日付以外の項目には触れません。
3. **表計算から貼り付け** … タブ区切りのデータを貼り付けます。
4. **メンバーリスト(OCR)** … 次項のとおり、PDFから大半の項目が埋まります。

#### メモからビジターホストを設定

Spreadingから取り込むと**役職**欄に、メンバーリスト(OCR)で読み込むと**メモ**欄に、
「ビジターホスト」等が入ります。
**「メモからビジターホストを設定」**ボタンを押すと、
役職欄・メモ欄のどちらかにその記載があるメンバーを、ビジターホスト設定に一括反映できます。
「ビジホス」のような略称も拾います。

### Spreadingから名簿を更新

`名簿システム` > `⚙️ 設定` > `Spreadingから名簿を更新`

SpreadingのAI連携（MCP）で取得したメンバー情報を、メンバー名簿に反映します。
**PDFの読み取りと違って文字化けや誤読が起きないため、こちらが使えるときはこちらが確実です。**

#### 最初に1回だけ：SpreadingのAI連携を許可する

この機能を使うには、先にClaude側でSpreadingとの連携を許可しておく必要があります。
一度許可すれば、次回からこの作業は不要です。

1. [claude.ai](https://claude.ai/) を開き、**設定 → コネクタ** を選びます。
2. **「カスタムコネクタを追加」** を押します。
3. URLに次のとおり入力します。

   ```
   https://active.spreading.tokyo/mcp_server*rpc
   ```

   > **`*`（アスタリスク）は誤字ではありません。** `/`（スラッシュ）に直すと動きません。
   > Spreadingが公開している設定にも、この形で書かれています。そのままコピーしてください。

4. Spreadingのアカウントでログインし、アクセスを許可します。
   マスター・メンバー・DNAチームのいずれの有効なアカウントでもかまいません。

**読み取り専用の連携です。**Spreading側のデータが書き換わることはありません。
パスワードなどの認証情報も連携の対象外です。

ただし、**メンバーと参加者の連絡先（メールアドレス・電話番号）も取得できる範囲に含まれます。**
チャプターの個人情報を扱うことになるので、運用上問題ないかご確認のうえで連携してください。

> **うまく登録できないとき**: SpreadingのMCPサーバーは「動的クライアント登録」に対応しておらず、
> 新しい方式（CIMD）を使っています。登録時にエラーになる場合はこれが原因である可能性が高く、
> こちら側では対処できません。Spreadingの提供元にお問い合わせください。

#### 使い方

1. Claudeなど、SpreadingのMCP連携を許可したAIに **`member_list` を実行して結果を見せて** と頼みます。
2. 返ってきた内容をコピーします。前後に説明文が混ざっていてもかまいません。
3. この画面の入力欄に貼り付けます。
4. まず **「変更内容を確認」** を押します。ここではまだ名簿は変わりません。
   誰のどの項目が、何から何に変わるかが一覧で出ます。
5. 問題なければ **「名簿に取り込む」** を押します。

#### 反映される項目

ふりがな・カテゴリー・会社名・役職・業種区分・入会日 の6つです。

**写真ファイル名・一言コメント・紹介してほしい人・協業したい人・メモ・更新日・更新期限日は
上書きしません。**すでに入っている内容はそのまま残ります。
Spreading側が空欄の項目も、上書きせずに元の内容を残します。

氏名で同じ人かどうかを判断します（スペースや全角半角の違いは無視します）。

#### Noの振り直しについて

**「Noを業種区分の並び順で振り直す」**にチェックを入れると、
業種区分の並び（企業サポート → 不動産関連 → …）とその中の並び順にしたがって、
1番から順に番号を振り直します。紙のメンバーリストと同じ並びになります。

ただし**番号が変わると、過去に作った割り振り表との対応が取れなくなります。**
必ず先に「変更内容を確認」を押して、いまの番号と変わらないかを見てから実行してください。
ふだんはチェックを外したままで問題ありません。

#### 退会された方について

名簿にはいるのにSpreadingの有効メンバーに見つからない方は、確認画面に一覧で出ます。
**自動では消しません。**退会された方かどうか、氏名の書き方が違うだけかを
ご自身で判断して、必要なら手で削除してください。
（自動で消すと、その方の写真や一言コメントも一緒に失われるためです）

### メンバーリスト(OCR)の更新

`名簿システム` > `⚙️ 設定` > `メンバーリスト(OCR)の更新`

メンバーリストのPDFを読み取って、**メンバー名簿の大半の項目を一度に埋めます**。
読み取るのは No・氏名・ふりがな・カテゴリー・会社名・役職・メモ、および
表の途中にある業種区分の見出し（企業サポート、不動産関連など）です。

**写真・一言コメント・日付など、PDFに載っていない項目は消えません。**
すでに入っている内容はそのまま残り、PDFにある項目だけが更新されます。
氏名またはNoが一致する行を同じ人とみなし、見つからなければ新しい行として追加します。
読み取りは **Gemini（AI）** が行うため、先に `⚙️ Gemini API・モデル設定` でAPIキーを設定しておいてください。

読み込み方法は2つあります。どちらでも結果は同じです。

- **方式A：ファイルを選んで解析** … パソコン内のPDFを選ぶだけ。ふだんはこちらでOKです。
- **方式B：ドライブのPDFを指定** … PDFをGoogleドライブに置き、その共有リンクかファイルIDを貼り付けます。
  ファイルが大きいときや、方式Aがうまくいかないときに使ってください。

> 読み取り結果は必ず確認してください。名前が間違っていたら「メンバーリスト」シートを直接修正すればOKです。

### メンバーブック(PDF)の更新

`名簿システム` > `⚙️ 設定` > `メンバーブック(PDF)の更新`

ビジターに配るメンバーブックのPDFをアップロードします。

- メールテンプレートの `{{memberbook}}` で自動的にこのPDFのURLが挿入されます。
- AI割り振り時にも、この内容がシナジー分析に使われます。

### AI参考資料の管理

`名簿システム` > `⚙️ 設定` > `AI参考資料`

AI自動割り振りのときに、メンバーブックと一緒にGeminiへ渡す参考資料を登録できます。
コンタクトサークルのアンケート結果や、メンバーのつながりたい相手リストなどを入れておくと、
割り振りの精度が上がります。

- PDF・画像・テキストなどに対応しています。複数登録できます。
- 一覧から不要になった資料を削除できます。

### 休会日の管理

`名簿システム` > `⚙️ 設定` > `休会日`

お盆・年末年始など、定例会がお休みの日を登録します。登録した日は定例会の候補日に表示されなくなります。

### メールテンプレート設定

`名簿システム` > `⚙️ 設定` > `メールテンプレート`

Visitor・Guest・Substituteの3種別ごとに、メールの件名と本文のひな形を設定します。CC/BCCも設定できます。

本文の中で以下の変数を使うと、送信時に自動で置き換わります。

| 書き方 | 置き換わる内容 |
|--------|--------------|
| `{{name}}` | 参加者の氏名 |
| `{{inviter}}` | 招待者の氏名 |
| `{{date}}` | 開催日 |
| `{{visitorlist}}` | ビジターリストPDFのURL |
| `{{memberbook}}` | メンバーブックPDFのURL |

### 割り振り表の特記事項設定

`名簿システム` > `⚙️ 設定` > `割り振り表の特記事項`

割り振り表PDFの下部に載せる注意書き（「当日の動き」など）を編集できます。

### ビジターホストの設定

`名簿システム` > `⚙️ 設定` > `ビジターホスト・優先順位`

ビジターホストを務めるメンバーにチェックを入れます。AI割り振り時に、ファシリテーターやオリエン担当として優先的に配置されます。

**一人ずつチェックしなくても、名簿から一括で設定できます。**
「メンバー名簿」画面の **「メモからビジターホストを設定」** を押すと、
役職欄またはメモ欄に「ビジターホスト」「ビジホス」等の記載があるメンバーを、
まとめてビジターホストに設定します。
（Spreadingから取り込むと役職欄に、メンバーリスト(OCR)で読み込むとメモ欄に入ります）

あわせて、メンバーごとに **優先順位（数値）** を設定できます。

- 数値が **小さいほど優先** され、ファシリテーター・オリエン担当に先に割り当てられます。
- **空欄のメンバーは最も優先度が低い**扱いになります。
- 割り振り表の待機リストでは、名前のうしろに `[1]` のように優先順位が表示されます。

### Gemini API・モデル設定

`名簿システム` > `⚙️ 設定` > `Gemini API・モデル`

AI自動割り振りとメンバーリスト(OCR)の読み取りに使うAPIキーを設定します。
Google AI Studio からキーを取得して入力してください。

### Gemini接続テスト

`名簿システム` > `🔧 動作確認` > `Gemini接続テスト`

APIキーが正しく設定され、Geminiと通信できるかをその場で確認できます。
AI割り振りやメンバーリスト(OCR)がうまく動かないときは、まずこれを実行してください。

- **✅ 成功** と出れば、APIキーと通信は正常です。
- **❌ 失敗** と出た場合は、表示されたエラー内容（HTTPコードなど）を確認してください。

---

## よくある質問

### Q: メニューに「名簿システム」が表示されません

→ スプレッドシートを **一度閉じて、開き直して** ください。開いたときにメニューが作られるので、それで表示されます。

### Q: CSVをドラッグ＆ドロップしても反応しません

→ Spreadingからダウンロードした **そのままのファイル** を使ってください。Excelなどで開いて保存し直すと、文字コードが変わってしまうことがあります。

### Q: 名簿を作ったあとに修正したくなりました

→ もう一度「1. CSVから名簿・PDF作成」を開いてください。画面の下に **「作成済みデータを再編集」** が表示されています。ここからシートを選べば、前回のデータを読み込んで修正・再作成できます。PDFのURLは変わりません。

### Q: メンバーリストの読み取り結果がおかしいです

→ PDFの画質によっては正しく読めないことがあります。
**「メンバー名簿」シート**を開いて、間違っている箇所を直接修正してください。
（以前の「メンバーリスト」シートは「メンバー名簿」に一本化されました）

→ **`⚙️ 設定` > `Spreadingから名簿を更新` が使えるなら、そちらをお使いください。**
データをそのまま受け取るので、読み間違いが起きません。

### Q: Spreadingから名簿を更新したら、番号が変わってしまいました

→ 「Noを業種区分の並び順で振り直す」にチェックが入っていた可能性があります。
このチェックを入れると、業種区分の並び順にしたがって1番から番号を振り直します。

番号が変わると、**過去に作った割り振り表との対応が取れなくなります。**
ふだんはチェックを外したままにして、押す前に必ず **「変更内容を確認」** で
番号が変わらないかご確認ください。

### Q: 退会したメンバーが名簿に残ったままです

→ **自動では消しません。**Spreadingの有効メンバーに見つからない方は、
確認画面に一覧で表示されるだけです。

自動で消してしまうと、その方の写真・一言コメント・紹介してほしい人なども
一緒に失われてしまうためです。退会された方かどうかをご自身で確認のうえ、
「メンバー名簿」シートの行を手で削除してください。

なお、**氏名の書き方がSpreadingと違うだけ**という場合もあります
（旧字体・スペースの有無など）。その場合は削除せず、氏名を合わせてください。

### Q: AI割り振りでエラーが出ます

→ 以下を確認してください。
1. `Gemini API・モデル設定` でAPIキーが正しく入っているか
2. メンバーブックPDFがアップロードされているか（なくても動きますが精度が下がります）
3. APIの利用制限に達していないか

### Q: メール送信でエラーが出ます

→ スプレッドシートの下のタブで **「〇〇参加者」シートが表示されている状態** で実行してください。別のシートが表示されていると、データが読み込めません。

### Q: PDFのURLを関係者に共有しても見られないと言われます

→ PDFは自動的に「リンクを知っている人は誰でも閲覧可」に設定されています。それでも見られない場合は、相手のブラウザでGoogleアカウントにログインした状態で開いてもらうか、シークレットモードで開いてみてください。

### Q: タブ（シート）が増えすぎて見づらいです

→ `5. シートの整理（アーカイブ）` を開いて「最近3開催日だけ残して一括アーカイブ」を実行してください。
データは消えず、タブの表示だけが減ります。元に戻したいときは `　└ アーカイブを全て表示に戻す` から復元できます。

### Q: 「サーバーエラー」と表示されて処理が進みません

→ 次の順に確認してください。
1. `⚙️ Gemini接続テスト` を実行して、Geminiとの通信が正常か確認する
2. メニューに `⚙️ Gemini接続テスト` が表示されない場合は、スクリプトが最新版に更新されていません。更新後にスプレッドシートを再読み込みしてください
3. それでも直らない場合は、Apps Scriptエディタの左メニュー「実行数」で、該当する処理の赤いエラー内容を確認してください

### Q: 消えてしまったシートを元に戻したいです

→ 削除ではなく**アーカイブ（非表示）**されている可能性が高いです。
`名簿システム` > `🗂 シートの整理` > `アーカイブを全て表示に戻す` で復元できます。
